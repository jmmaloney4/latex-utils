name: 📌 add to project
on:
  pull_request:
    types: [opened]
  issues:
    types: [opened]
permissions:
  contents: read
jobs:
  add-to-project:
    uses: jmmaloney4/sector7/.github/workflows/add-to-project.yml@main
    with:
      runs-on: self-hosted
